# Utilisé uniquement en build de production (npm run build).
# En dev local, laisser vide : le proxy Vite (vite.config.js) redirige /api vers
# http://127.0.0.1:8000 automatiquement.
VITE_API_BASE_URL=https://api.tondomaine.fr/api
